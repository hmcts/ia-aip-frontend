{% macro timeline(history) %}
  <h3 class="govuk-heading-m">{{ i18n.pages.overviewPage.timeline.completed }}</h3>
  {% for event in history %}
    <div class="govuk-!-margin-bottom-3">
      <h4 class="govuk-heading-s">{{ event.title }}</h4>
      <div class="inline-flow">
        <div class="timeline-tick"></div>
        <div><span class='govuk-body govuk-!-font-weight-bold'>{{ event.date }}</span> - {{ event.text }}</div>
      </div>
      {% for link in event.links %}
        <div class="timeline-margin-left">
          <h3 class="govuk-heading-s govuk-!-margin-top-3 ">{{ link.title }}</h3>
          <a href="{{ link.href | eval }}" class="govuk-link govuk-!-margin-bottom-1">{{ link.text }}</a>
        </div>
      {% endfor %}
    </div>
  {% endfor %}
{% endmacro %}
