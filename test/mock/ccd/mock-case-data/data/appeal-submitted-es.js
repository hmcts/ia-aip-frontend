const { appealSubmittedCaseData } = require("../index");

const appealSubmittedCaseDataES = {
  "total": 1,
  "cases": [
    appealSubmittedCaseData
  ]
}

module.exports = { ...appealSubmittedCaseDataES };
