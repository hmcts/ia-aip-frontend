{% extends '../layout.njk' %}


{% block content %}
<h1>{{ title }}</h1>
<p class="govuk-body">{{ titleText }}</p>
{% for item in textAndBullets %}
    <p>{{ item.title }}</p>
    <h1>{{ item.text }}</h1>
{% endfor %}
{% endblock %}